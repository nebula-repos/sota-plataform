---
description: Validate the project (Lint and Build)
---

// turbo
1. Clean the project by running `rm -rf .next`.
2. Restructure the "just done feat" (e.g., ensure files are in correct directories, logic is encapsulated, and rules are followed).
// turbo
3. Run `pnpm lint`.
// turbo
4. Run `pnpm build`.