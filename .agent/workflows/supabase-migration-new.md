---
description: Create a new Supabase migration
---

1. Ask the user for a descriptive name for the migration (e.g., `add_users_table`).
// turbo
2. Run `supabase migration new {migration_name}`.
3. Notify the user that the migration file has been created in `supabase/migrations` and remind them to add their SQL to it.